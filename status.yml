- name: satellite-registration
  hosts: all
  become: yes

  tasks:

   - name: Uptime checking
     command: uptime
     register: out
   - debug: msg="{{ out.stdout }}"
   
   - name: create file
     command: touch /tmp/123vino
     register: va
   - debug: msg="{{ va.stdout }}"
